import {componentList as nativeBaseUI, wrapper as nativeBaseWrapper} from './libs/native-base.js';

export default [
  {
    name:"Native Base",
    componentList:nativeBaseUI,
    wrapper:nativeBaseWrapper
  }
]
